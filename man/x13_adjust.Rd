% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/seasonal_adjust.R
\name{x13_adjust}
\alias{x13_adjust}
\title{X-13-ARIMA-SEATS wrapper for a single monthly series}
\usage{
x13_adjust(df, force_additive = FALSE)
}
\arguments{
\item{df}{Tibble with \code{month_end} (Date, first or last of month) and
\code{value} (numeric).}

\item{force_additive}{If \code{TRUE}, skip the positivity check and use
additive transform unconditionally.}
}
\value{
The input tibble with an added \code{value_sa} numeric column.
}
\description{
Thin, tidy wrapper around \code{seasonal::seas()}. Accepts a long tibble
(\code{month_end}, \code{value}), returns the same tibble with a \code{value_sa}
column. Handles the awkward corners so callers don't have to:
}
\details{
\itemize{
\item \strong{Short series} (< 36 monthly observations). X-13 refuses to fit;
we fall back to \code{value_sa = value} and log a warning. 36 months is
X-13's own minimum for the default spec; with only 2 years of data
the seasonal pattern isn't identified anyway.
\item \strong{Non-positive values}. X-13 is multiplicative by default. If any
value <= 0 we switch to additive (\code{transform.function = "none"}).
For our data (export values, tonnage), true zeros are rare but can
occur in small-port months.
\item \strong{Gaps}. X-13 requires a regular ts. We reindex to a full monthly
grid and carry \code{NA} into \code{seasonal::seas()}, which accepts them.
}
}
