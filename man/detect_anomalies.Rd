% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/anomalies.R
\name{detect_anomalies}
\alias{detect_anomalies}
\title{Detect >threshold-sigma departures from the seasonal daily norm}
\usage{
detect_anomalies(
  portwatch,
  cfg,
  as_of = Sys.Date(),
  lookback_days = 28L,
  window_days = 7L,
  threshold = 2
)
}
\arguments{
\item{portwatch}{Daily tonnage tibble.}

\item{cfg}{Config list.}

\item{as_of}{Reference date.}

\item{lookback_days}{How many recent days to inspect.}

\item{window_days}{Half-width of the seasonal-norm smoothing window.}

\item{threshold}{\verb{|z|} flag threshold.
Also overwrites \code{mart.latest_anomalies} with the detected rows so
the briefing and dashboard can query them without re-running the
detection.}
}
\value{
Tibble: \code{obs_date}, \code{port_id}, \code{commodity}, \code{tonnage},
\code{expected}, \code{sd}, \code{z_score}, sorted descending by \verb{|z_score|}.
}
\description{
For each (port, commodity), build a seasonal norm by pooling
observations from the training window (2019-\code{cfg$sample$train_end})
across a +/-\code{window_days}-day window around each day-of-year. Z-score
each observation in the last \code{lookback_days} against its seasonal
mean and SD; return rows with \verb{|z| > threshold} sorted by \verb{|z|} desc.
}
