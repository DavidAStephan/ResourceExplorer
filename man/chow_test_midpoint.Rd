% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stability.R
\name{chow_test_midpoint}
\alias{chow_test_midpoint}
\title{Chow test for a structural break at the midpoint of the training sample}
\usage{
chow_test_midpoint(fit, data, min_n_per_half = 8L)
}
\arguments{
\item{fit}{\code{lm} object from \code{\link[=fit_bridge_one]{fit_bridge_one()}}.}

\item{data}{Training tibble used to fit \code{fit}.}

\item{min_n_per_half}{Minimum observations per half-sample.}
}
\value{
List with \code{fstat}, \code{pval}, \code{df1}, \code{df2}, \code{n}, \code{break_at}.
}
\description{
Splits the training data in half (by \code{quarter_end} order), refits the
same lm on each half, and reports the standard Chow F-stat for the
null "no break".
}
\details{
\deqn{F = \frac{(SSR_{pooled} - SSR_1 - SSR_2)/k}{(SSR_1 + SSR_2)/(n - 2k)}}

Returns NA when either half is below \code{min_n} observations -- we want
an honest test, not one fit on 6 points.

Built-in base R so we don't pick up an external dependency
(\code{strucchange}, \code{lmtest}) for one test. The Chow setup is the right
choice anyway at our sample size: a single known break at the
midpoint is the highest-power test you can run with N ≈ 23.
}
\keyword{internal}
