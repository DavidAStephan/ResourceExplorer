% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/warehouse.R
\name{warehouse}
\alias{warehouse}
\alias{warehouse_init_schema}
\alias{wh_path}
\alias{wh_exists}
\alias{wh_write}
\alias{wh_read}
\alias{wh_append}
\title{RDS-backed warehouse}
\usage{
warehouse_init_schema(cfg)

wh_path(name, cfg)

wh_exists(name, cfg)

wh_write(name, tbl, cfg)

wh_read(name, cfg)

wh_append(name, tbl, cfg, keys = NULL)
}
\arguments{
\item{cfg}{Config list. Uses \code{cfg$paths$warehouse_dir}.}

\item{name}{Table name (snake_case, no extension).}

\item{tbl}{Tibble / data.frame to write.}

\item{keys}{Character vector of columns to dedupe on. If \code{NULL}, rows
are concatenated without dedup.}
}
\description{
Replaces the previous DuckDB warehouse (not on the work-laptop package
allow-list). Each "table" is a single \code{.rds} file under
\code{cfg$paths$warehouse_dir}. Data volumes are small (≤ tens of thousands
of rows), so \code{readRDS} / \code{saveRDS} are more than fast enough and add
zero compiled-package surface area.
}
\details{
Public API:
\itemize{
\item \code{warehouse_init_schema(cfg)} — ensure the warehouse directory exists.
Returns the directory path invisibly (target-compatible signature).
\item \code{wh_write(name, tbl, cfg)} — overwrite the named table.
\item \code{wh_append(name, tbl, cfg)} — append rows, deduping by any shared keys.
\item \code{wh_read(name, cfg)} — read (or return \code{NULL} if absent).
\item \code{wh_exists(name, cfg)} — path predicate.
}

Table names should be short, filesystem-safe snake_case strings, e.g.
\code{"raw_portwatch_tonnage_daily"}, \code{"mart_nowcast_history"}.
}
