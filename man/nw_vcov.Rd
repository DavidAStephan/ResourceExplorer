% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hac.R
\name{nw_vcov}
\alias{nw_vcov}
\title{Newey-West HAC variance-covariance matrix}
\usage{
nw_vcov(fit, lag = 3L, prewhite = FALSE)
}
\arguments{
\item{fit}{An \code{lm} object.}

\item{lag}{Non-negative integer bandwidth \eqn{L}. \code{lag = 0} returns
the heteroscedasticity-consistent (White) covariance.}

\item{prewhite}{Must be \code{FALSE} (pre-whitening not implemented; we
don't need it for our bridge regressions).}
}
\value{
A k-by-k numeric matrix with the regressor names as dimnames.
}
\description{
Heteroscedasticity- and autocorrelation-consistent (HAC) covariance
matrix with Bartlett kernel. Hand-rolled replacement for
\code{sandwich::NeweyWest()} because \code{{sandwich}} is not on the work-laptop
package allow-list.
}
\details{
Formula (no pre-whitening):

\deqn{V_{HAC} = n (X'X)^{-1} \Omega (X'X)^{-1}}

with
\deqn{\Omega = \Gamma_0 + \sum_{h=1}^{L} w_h (\Gamma_h + \Gamma_h')}
\deqn{w_h = 1 - h / (L+1), \qquad \Gamma_h = \tfrac{1}{n} \sum_{t=h+1}^{n} u_t u'_{t-h}}

where \eqn{u_t = x_t e_t} is the per-observation score for OLS. This
matches \code{sandwich::NeweyWest(fit, lag = L, prewhite = FALSE)} to
machine precision (see \code{tests/testthat/test-hac.R}).
}
