% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/backtest.R
\name{backtest_rmse}
\alias{backtest_rmse}
\title{Walk-forward backtest of the bridge ensemble vs ABS 5302.0 actuals}
\usage{
backtest_rmse(features, abs_5302, cfg)
}
\arguments{
\item{features}{Long feature tibble from \code{\link[=build_features]{build_features()}}.}

\item{abs_5302}{Quarterly ABS tibble from \code{\link[=fetch_abs_5302]{fetch_abs_5302()}}.}

\item{cfg}{Config list.}
}
\value{
Tibble: \code{quarter_end}, \code{actual}, \code{point_estimate},
\code{naive_srw}, \code{err}, \code{err_naive}.
}
\description{
\strong{Scheme.} Expanding-window refit. For each quarter \code{Q} from
\code{cfg$sample$valid_start} to the last quarter for which 5302.0 has
landed, we:
}
\details{
\enumerate{
\item Cut the feature panel to \verb{month_end <= (start_of_Q - 1 month)} --
i.e. the last observation is the month immediately preceding Q.
\item Refit all commodity bridges via \code{\link[=fit_bridge]{fit_bridge()}} on that window.
\item Build the Q prediction feature frame (the three months of Q from
\code{features}). Feed it to \code{\link[=predict_bridge]{predict_bridge()}}, which recursively
uses its own predictions as lags from month 2 onward.
\item Sum monthly predictions across commodities to a quarterly
current-price total; chain-volume convert via
\code{\link[=apply_chain_volume]{apply_chain_volume()}}.
\item Record actual (from 5302.0), point estimate, and the
seasonal-random-walk benchmark \verb{actual_\{Q-4\}}.
}

\strong{Benchmark.} Seasonal random walk at quarterly frequency:
\verb{y_hat_Q = actual_\{Q-4\}}. This is the "no-skill" anchor the brief
mandates. The success target is RMSE >= 30\% below the benchmark.

\strong{Window choice.} Expanding (not rolling). The brief names a
2019-2023 train sample and the economic regime is better captured
by more data than by a fixed-width window -- particularly important
for LNG given the 2022 shock. Switch to rolling in Phase 4 if
structural breaks become a concern.
}
