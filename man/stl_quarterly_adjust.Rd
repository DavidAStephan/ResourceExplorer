% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/seasonal_adjust.R
\name{stl_quarterly_adjust}
\alias{stl_quarterly_adjust}
\title{STL-based quarterly seasonal adjustment for a single series}
\usage{
stl_quarterly_adjust(df, force_additive = FALSE)
}
\arguments{
\item{df}{Tibble with \code{quarter_end} (Date) and \code{value} (numeric).}

\item{force_additive}{If \code{TRUE}, skip positivity check and use
additive decomposition unconditionally.}
}
\value{
The input tibble with an added \code{value_sa} numeric column.
}
\description{
Wraps \code{stats::stl()} with \code{s.window = "periodic"} and \code{robust = TRUE},
frequency = 4 (quarters per year). Accepts a long tibble with columns
\code{quarter_end} (Date, last day of quarter) and \code{value} (numeric),
returns the same tibble with a \code{value_sa} column.
}
\details{
Handles the awkward corners:
\itemize{
\item \strong{Short series} (< 8 quarterly observations = 2 full cycles). STL
needs at least two periods to identify the seasonal. Fall back to
\code{value_sa = value} and log a warning.
\item \strong{Non-positive values.} Multiplicative adjustment requires logs of
positive values. If any \code{value <= 0} we switch to additive.
\item \strong{Gaps.} STL needs a regular \code{ts}. We reindex to a full quarterly
grid and linearly interpolate single-quarter gaps.
}

Replaced X-13-ARIMA-SEATS (\code{{seasonal}}) because its Fortran binary is
not on the work-laptop allow-list. STL loses X-13's calendar /
trading-day adjustments, but at quarterly frequency and for bulk
commodity tonnage the trading-day signal is tiny.
}
