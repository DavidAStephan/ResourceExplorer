% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tonnage_extrapolation.R
\name{extrapolate_quarter_tonnage}
\alias{extrapolate_quarter_tonnage}
\title{Estimate full-quarter tonnage from a partially observed quarter}
\usage{
extrapolate_quarter_tonnage(portwatch, ports_meta, cfg, as_of = Sys.Date())
}
\arguments{
\item{portwatch}{Daily tonnage tibble from \code{\link[=fetch_portwatch_tonnage]{fetch_portwatch_tonnage()}}.}

\item{ports_meta}{Port metadata, for the commodity mapping.}

\item{cfg}{Config list.}

\item{as_of}{Date; defaults to \code{Sys.Date()}.}
}
\value{
Tibble: \code{commodity}, \code{month_end}, \code{tonnage_est},
\code{share_observed}, \code{status}.
}
\description{
Given the current date \code{as_of} sitting inside quarter Q, return
one row per (commodity, month in Q) with a best-estimate tonnage
plus a \code{status} flag (\code{observed} / \code{partial} / \code{estimated}).
}
\details{
\strong{Partial-month scale-up} uses the 2019-training day-of-month share.
If through day d we've observed a fraction \code{share(d)} of the typical
month's tonnage, then estimated full-month tonnage is
\code{observed / share(d)}. For commodities whose shipping cadence isn't
uniform (coal, LNG bulk shipments) this beats a flat pro-rata.

\strong{Unobserved future months} use \verb{seasonal_avg x pace}, where \code{pace}
is the ratio of the most-recent complete month's tonnage to its own
seasonal average. So a pickup in observed pace carries forward.

\strong{PortWatch lag.} IMF's daily-ports endpoint refreshes with a
multi-day lag; on any given Tuesday, the latest available row is
typically 7-14 days back. The partial-month scale-up therefore
anchors on \code{effective_today = min(as_of, max(obs_date))} rather than
the calendar date \code{as_of}, so we never divide actually-observed
tonnage by an expected-share computed for days the data doesn't cover
yet. Backtests still bound \code{effective_today} at the requested \code{as_of}.

\strong{Sanity clip.} A partial-month estimate that lands more than \verb{3x}
away from \code{seasonal_norm * pace} in either direction is clipped to
the seasonal-norm value and a warning is logged. Catches partial-
month divisions that go pathological on tiny day-counts.
}
