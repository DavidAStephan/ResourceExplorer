% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/combination.R
\name{combined_log_residuals}
\alias{combined_log_residuals}
\title{Combine in-sample log V hat across components into a single residual
series, which the nowcast bootstrap samples from.}
\usage{
combined_log_residuals(pm)
}
\description{
For each commodity's production-model bundle:
\itemize{
\item For each component fit, compute fitted \code{log_volume} per training
quarter (back-transforming \code{bojo} from the YoY-Δ scale).
\item Align on \code{quarter_end} across components; drop quarters where any
active component lacks a fit.
\item Compute combined \verb{yhat_log = Σ_i w_i * yhat_log_i}.
\item Residual = \verb{log V_Q (observed) - yhat_log}.
}
}
\details{
The returned vector preserves the existing log-space residual
bootstrap (no scale change); a single-spec production choice gives
exactly the same residuals as the previous per-spec code.
}
\keyword{internal}
