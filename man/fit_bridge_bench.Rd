% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bridge.R
\name{fit_bridge_bench}
\alias{fit_bridge_bench}
\title{Fit per-commodity bridge regressions (candidate-bench version)}
\usage{
fit_bridge_bench(features, cfg)
}
\arguments{
\item{features}{Quarterly feature tibble from \code{\link[=build_features]{build_features()}}.}

\item{cfg}{Config list. Uses \code{cfg$sample$train_end}, \code{cfg$commodities},
\code{cfg$bridge$hac_lag}, \code{cfg$bridge$min_n}, and the candidate list
\code{cfg$bridge$candidates} (default: \code{c("aggregate", "midas", "bojo")}).}
}
\value{
Long named list keyed by \code{"<commodity>__<spec>"}. Each element
is a list with \code{fit}, \code{vcov_hac}, \code{residuals}, \code{spec}, \code{commodity},
and \code{diagnostics}.
}
\description{
Each commodity is fit under several candidate specifications. The
production nowcast either picks the single best by out-of-sample
RMSE or averages across candidates (see \code{R/combination.R} +
\code{\link[=select_production_models]{select_production_models()}}). Backtest does the model-selection
work; this function just fits each candidate against the available
training data.
}
\details{
Supported specs:
\itemize{
\item \strong{aggregate} (3 params + intercept), the parsimonious variant:
\deqn{\log V_{c,Q} = \beta_0 + \beta_T \, \Delta_{Q,Q-4}\log T_{c,Q}
                      + \beta_4 \log V_{c,Q-4} + \varepsilon_{c,Q}}
\item \strong{midas} (5 params + intercept), the unrestricted-MIDAS variant:
\deqn{\log V_{c,Q} = \beta_0 + \sum_{m=1}^{3} \beta_m \,
                        \Delta_{Q,Q-4}\log T_{c,Q,m}
                      + \beta_4 \log V_{c,Q-4} + \varepsilon_{c,Q}}
\item \strong{bojo} (2 params, after Furukawa & Hisano 2022 / Del-Rosario &
Quach 2024), the pure YoY-on-YoY form. Algebraically equivalent
to the \code{aggregate} spec with \code{beta_4} constrained to 1:
\deqn{\Delta_{Q,Q-4}\log V_{c,Q} = \beta_0
                        + \beta_T \, \Delta_{Q,Q-4}\log T_{c,Q}
                        + \varepsilon_{c,Q}}
}

Each fit reports the same diagnostics tibble plus a \code{beta_lag4_eq_1_pval}
from a Wald test (NA for the \code{bojo} spec, where the restriction is
imposed by construction).
}
