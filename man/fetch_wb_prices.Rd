% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ingest_wb_prices.R
\name{fetch_wb_prices}
\alias{fetch_wb_prices}
\title{Fetch the World Bank Pink Sheet monthly commodity-price workbook}
\usage{
fetch_wb_prices(cfg, db_ready)
}
\arguments{
\item{cfg}{Config list.}

\item{db_ready}{Dependency handle (unused; kept for signature parity
with the other ingest functions).}
}
\value{
Long tibble: \code{quarter_end}, \code{commodity}, \code{price}, \code{log_price},
\code{ingested_at}. One row per (commodity, quarter).
}
\description{
The Pink Sheet is the World Bank's monthly commodity-price publication,
covering ~70 series back to 1960. We pull:
}
\details{
\itemize{
\item \verb{Iron ore, cfr spot} (China CFR import benchmark) -> mapped to
\code{iron_ore}.
\item \verb{Coal, Australian} (Newcastle benchmark, thermal coal) -> mapped
to both \code{coal_met} and \code{coal_thermal}. Newcastle is a \emph{thermal}
benchmark; met coal isn't in the Pink Sheet. Using it as the
met-coal proxy is imperfect (the two commodities have different
demand cycles); the bridge candidate-bench is what decides
whether the price term actually helps each sub-commodity.
}

The Pink Sheet's URL changes with each annual reorganisation. We
probe a small list of candidate URLs in reverse-chronological order
and use the first one that returns a 200; falls back to the on-disk
cache when offline.
}
