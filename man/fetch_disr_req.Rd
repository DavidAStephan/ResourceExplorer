% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ingest_disr.R
\name{fetch_disr_req}
\alias{fetch_disr_req}
\title{Fetch the DISR Resources and Energy Quarterly historical-data workbook}
\usage{
fetch_disr_req(cfg, db_ready)
}
\arguments{
\item{cfg}{Config list; uses \code{cfg$disr$sheet} (default \code{"16"}),
\code{cfg$disr$rows} (named list mapping commodity -> row number or
vector of rows), \code{cfg$disr$url_override} (optional).}

\item{db_ready}{Dependency handle (unused; kept for signature parity).}
}
\value{
Long tibble: \code{quarter_end}, \code{commodity}, \code{tonnes_Mt},
\code{source_url}, \code{ingested_at}.
}
\description{
The Australian Department of Industry, Science & Resources (DISR)
publishes the \strong{Resources and Energy Quarterly (REQ)} in March, June,
September and December. Each release ships a "historical data" xlsx
workbook whose \strong{Sheet 16} contains quarterly commodity exports by
physical volume (kt / Mt / ML depending on commodity).
}
\details{
URL pattern (as at 2026-04):
\verb{https://www.industry.gov.au/sites/default/files/YYYY-MM/resources-and-energy-quarterly-MONTH-YYYY-historical-data.xlsx}
— where \code{YYYY-MM} and the \code{MONTH-YYYY} slug match the release date,
e.g. \code{2025-12} / \code{december-2025}.

Because the URL changes quarterly, \code{\link[=disr_latest_url]{disr_latest_url()}} probes candidate
URLs in reverse-chronological order (starting with the current
quarter) and returns the newest that responds 200.

The result is a long tibble: \code{commodity}, \code{quarter_end}, \code{tonnes_Mt}.
Iron ore (published in kt) is converted to Mt. Metallurgical + thermal
coal are summed into a single \code{coal} row.
}
